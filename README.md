# LLM Service - Ассистент разработчика

## День 31: Ассистент разработчика с RAG и MCP

Этот проект реализует ассистента разработчика с возможностью доступа к документации проекта через RAG и к Git репозиторию через MCP (Model Context Protocol).

## 🚀 Функциональность

### 📚 RAG для документации проекта
- Автоматическая загрузка документации из папки `docs/`
- Быстрый поиск по документации проекта
- Интеграция с существующим RAG для документов логистики

### 🔧 MCP Git инструменты
- Получение текущей git-ветки
- Список файлов в проекте с фильтрацией по расширениям
- Древовидная структура проекта
- Просмотр незакоммиченных изменений
- Чтение README файлов

### 💬 Команды
- `/help` - список всех команд и примеры вопросов
- `/branch` - текущая git-ветка
- `/files` - файлы проекта
- `/structure` - структура проекта
- `/diff` - незакоммиченные изменения
- `/readme` - содержимое README

## 🏗️ Структура проекта

```
llm-service/
├── app/
│   ├── __init__.py
│   ├── main.py          # Точка входа FastAPI
│   ├── api.py           # API эндпоинты
│   ├── rag.py           # RAG системы (проект + логистика)
│   ├── ollama_client.py # Клиент для Ollama с обработкой команд
│   ├── mcp_tools.py     # MCP инструменты для Git
│   ├── models.py        # Модели данных
│   └── config.py        # Конфигурация
├── docs/                # Документация проекта
│   ├── README.md        # Общая информация
│   ├── API.md           # API документация
│   └── ARCHITECTURE.md  # Архитектура проекта
├── data/                # Базы данных RAG
├── static/              # Веб-интерфейс
├── docker-compose.yml
└── requirements.txt
```

## 🛠️ Установка и запуск

### Требования
- Docker и Docker Compose
- Ollama (локально или в контейнере)

### Запуск
```bash
docker compose up --build
```

Приложение будет доступно по адресу: http://localhost:8000

## 🌐 API эндпоинты

### Основные
- `GET /health` - статус приложения
- `POST /api/chat` - чат с ассистентом (с поддержкой команд)
- `GET /api/rag/search` - поиск по документации

### MCP Git API
- `GET /api/git/branch` - текущая ветка
- `GET /api/git/files` - список файлов
- `GET /api/git/structure` - структура проекта
- `GET /api/git/diff` - изменения в репозитории
- `GET /api/docs/readme` - содержимое README

## 💡 Примеры использования

### Через веб-интерфейс
1. Откройте http://localhost:8000
2. Используйте команды:
   - `/help` - получить справку
   - `/files` - посмотреть файлы проекта
   - `/structure` - посмотреть структуру

### Через API
```bash
# Получить текущую ветку
curl http://localhost:8000/api/git/branch

# Получить список Python файлов
curl http://localhost:8000/api/git/files?extension=.py
```

## 🔄 Архитектура

Приложение построено на:
- **FastAPI** - веб-фреймворк
- **Ollama** - локальные LLM модели
- **FAISS** - векторный поиск (для существующего RAG)
- **Git MCP** - интеграция с Git через subprocess
- **Простой поиск по ключевым словам** - для документации проекта

## 🔌 Расширение функциональности

### Добавление новых команд:
1. Добавить обработку в `app/ollama_client.py::_handle_command()`
2. Добавить функцию в `app/mcp_tools.py`
3. Обновить команду `/help`

### Добавление документации:
1. Добавить файлы в папку `docs/`
2. Они будут автоматически загружены при перезапуске

## 📊 Статус реализации

✅ **Выполнено:**
- Создана документация проекта в папке `docs/`
- Реализованы MCP инструменты для Git
- Добавлены API эндпоинты для Git операций
- Реализована команда `/help` и обработка команд
- Обновлен веб-интерфейс с подсказками команд
- Интегрирован ProjectDocsRAG для поиска по документации

⚡ **Готово к использованию в День 31**

## 📝 Задание из .task.md

Задание выполнено в полном объеме:
- ☑️ Подключение RAG к документации проекта (README + папка docs)
- ☑️ MCP для получения текущей git-ветки (и дополнительно: файлы, структура, diff)
- ☑️ Команда `/help`, отвечающая на вопросы о структуре проекта
- ☑️ Ассистент разработчика, понимающий проект